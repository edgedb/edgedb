#!INSTANCE ConceptLinkTypes (semantix.lib.datasources.sql.Sql)

cache: memory

filters:
    - format: dict

source: |
    SELECT
            id,
            name
        FROM
            concept_map_link_type
