%SCHEMA semantix.datasources.schemas.Sql
%NAME AtomList
---
params:

filters:
    - format: dict

source: |
    SELECT
            c.id AS id,
            c.name AS name,
            c.title AS title,
            c.description AS description,
            c.automatic AS automatic
        FROM
            caos.atom c
